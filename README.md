# buchin/searchterm
Detect search keywords if referer coming from search engine

## Usage
```php
<?php
use Buchin\SearchTerm\SearchTerm;

$term = SearchTerm::get();

/*
Example result:
(string) "ketela mambu"
*/
```